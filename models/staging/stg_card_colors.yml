version: 2

models:
  - name: stg_card_colors
    description: "Vista staging que contiene una fila por cada color asociado a cada carta."
    columns:
      - name: card_id
        description: "Identificador único de la carta."
        tests:
          - not_null
      - name: color
        description: "Color individual de la carta."
        tests:
          - not_null
